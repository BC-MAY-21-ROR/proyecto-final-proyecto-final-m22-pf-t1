<p style="color: green"><%= notice %></p>

<%= render @stylist %>

<div>
  <%= link_to "Edit this stylist", edit_stylist_path(@stylist) %> |
  <%= link_to "Back to stylists", stylists_path %>

  <%= button_to "Destroy this stylist", @stylist, method: :delete %>
</div>
