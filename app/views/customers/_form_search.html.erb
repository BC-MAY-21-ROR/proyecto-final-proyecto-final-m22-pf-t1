

<%= form_with url: customers_desactivate_path, method: :get do |form|  %>

  <%= form.label :query, "Cliente:" %>

  <%= form.search_field :_query,  placeholder: "Nombre completo ó dni" %>

    <%= form.submit "Search", class: 'btn btn-primary '  %>
<% end %>


<%= form_with url: customers_desactivate_path, method: :get do |form|  %>

  <%= form.label :query, "Cliente:" %>

  <% if @customers %>
  <%= form.collection_select :customer_id, @customers.order(:full_name), :id, :full_name %>
  <% end %>


  <%= form.submit "desactivar", class: 'btn btn-primary '  %>
<% end %>

