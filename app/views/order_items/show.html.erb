<p style="color: green"><%= notice %></p>


<h1>Artículos</h1>

<table>
  <thead>
    <tr>
      <th>Factura</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_items.each do |item| %>
      <tr>
        <td><%= item.invoice_id %></td>
        <td><%= item.invoice.name %></td>
        <td><%= link_to 'Mostrar factura', [@invoice] %></td>
        <td><%= link_to 'Editar artículo', edit_invoice_order_item_path(@invoice, order_item) %></td>
        <td><%= link_to 'Eliminar artículo', [@invoice, order_item], method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_shark_post_path(@shark) %>

<div>
  <%= link_to "Edit this order item", edit_order_item_path(@order_item) %> |
  <%= link_to "Back to order items", order_items_path %>

  <%= button_to "Destroy this order item", @order_item, method: :delete %>
</div>
