<%= render "layouts/sidenav" %>
<div class="row pe-5 justify-content-center mt-3">
      <div class="col-lg-4 grid-margin stretch-card mt-2 ">
        <div class="card border border-white rounded-lg mt-2 mb-2">
            <div class="header">
              <h1 class="text-center">New order item</h1>
            </div>
            <%= render "form", order_item: @order_item %>

            <br>

            <div class="d-flex justify-content-center">
              <%= link_to "Back to order items", invoice_order_items_path(@invoice), class:"btn btn-secondary float-end m-2"%>
                <%= link_to "Back to invoices", invoices_path, class:"btn btn-secondary float-end m-2"%>
            </div>
        </div>
      </div>