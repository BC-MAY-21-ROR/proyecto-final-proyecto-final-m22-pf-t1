<%= render "layouts/sidenav" %>
<h1>Editing order item</h1>
  <%= render "form", order_item: @order_item %>

  <div>
    <%= link_to "Show this order item", [@invoice, @order_item]  %> |
    <%= link_to "Back to order items", invoice_order_items_path(@invoice) %>
    <%= link_to 'Edit', invoice_order_items_path(@invoice, @order_item) %> |
  <%= link_to 'Back', invoice_order_items_path(@invoice) %>
  </div>
