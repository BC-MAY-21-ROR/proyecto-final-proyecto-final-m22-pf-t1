<nav class="navbar navbar-expand-lg bg-light mb-4">
  <div class="container">
    <a href="/">
      <%=image_tag "logo", class:"d-block mx-lg-auto img-fluid navbar-brand", width:"160", height:"80" %>
    </a> 
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "Dashboard",  dashboard_index_path, class:"nav-link active" %>
        <% end %>
        </li>
      </ul>
      <form class="d-flex">
        <% if user_signed_in? %>
        <button class="btn btn-outline-prymary me-2" disabled type="submit"><%= current_user.email %></button>
        <%=  link_to  "Cerrar sesión", destroy_user_session_path, method: :delete, class:"btn btn-secondary", data: { turbo_method: :delete } %><br />
        <% else %>
        <%= link_to "Prueba Gratis", new_user_registration_path, class:"btn btn-success me-2" %><br />
        <%= link_to "Ingresar", new_user_session_path, class:"btn btn-outline-primary" %><br />
        <% end %>   
      </form>
    </div>
  </div>
</nav>
